{% extends 'base.html.twig' %}

{% block title %}PointRang{% endblock %}

{% block body %}
    <h1>PointRang</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ point_rang.id }}</td>
            </tr>
            <tr>
                <th>Point</th>
                <td>{{ point_rang.point }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ point_rang.rang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_point_rang_index') }}">back to list</a>

    <a href="{{ path('app_point_rang_edit', {'id': point_rang.id}) }}">edit</a>

    {{ include('point_rang/_delete_form.html.twig') }}
{% endblock %}
