{% extends 'admin/base.html.twig' %}

{% block title %}Travaux index{% endblock %}

{% block body %}
    <div class="card">
        <div class="header">
            <h2>
                Liste de vos devis
            </h2>
            <ul class="header-dropdown m-r--5">
                <li class="dropdown">
                    <a href="javascript:void(0);" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                        <i class="material-icons">more_vert</i>
                    </a>
                    <ul class="dropdown-menu pull-right">
                        <li><a href="javascript:void(0);" class=" waves-effect waves-block">Action</a></li>
                        <li><a href="javascript:void(0);" class=" waves-effect waves-block">Another action</a></li>
                        <li><a href="javascript:void(0);" class=" waves-effect waves-block">Something else here</a></li>
                    </ul>
                </li>
            </ul>
        </div>
        <div class="body table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>Description</th>
                        <th>PrixUnitaire</th>
                        <th>Unite</th>
                        <th>Code</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for travaux in travauxes %}
                        <tr>
                            <td>{{ travaux.description }}</td>
                            <td>{{ travaux.prixUnitaire|number_format(2, ',', ' ') }} Ar </td>
                            <td>{{ travaux.getUnite().getDesignation() }}</td>
                            <td>{{ travaux.code }}</td>
                            <td>
                                {# <a href="{{ path('app_travaux_show', {'id': travaux.id}) }}"><button>show<</a> #}
                                <a href="{{ path('app_travaux_edit', {'id': travaux.id}) }}"><button class="btn btn-warning">Modifier</button></a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4">Vous n'avez pas encore de travaux actuellement</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
            <div =calss="pagin">
                {{ knp_pagination_render(travauxes)}}
            </div>
        </div>
    </div>

    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>PrixUnitaire</th>
                <th>Code</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for travaux in travauxes %}
            <tr>
                <td>{{ travaux.id }}</td>
                <td>{{ travaux.description }}</td>
                <td>{{ travaux.prixUnitaire }}</td>
                <td>{{ travaux.code }}</td>
                <td>
                    <a href="{{ path('app_travaux_show', {'id': travaux.id}) }}">show</a>
                    <a href="{{ path('app_travaux_edit', {'id': travaux.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

    {# <a href="{{ path('app_travaux_new') }}">Create new</a> #}
{% endblock %}
