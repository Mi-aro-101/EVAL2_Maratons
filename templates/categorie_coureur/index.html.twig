{% extends 'base.html.twig' %}

{% block title %}CategorieCoureur index{% endblock %}

{% block body %}
    <h1>CategorieCoureur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>NomCategorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_coureur in categorie_coureurs %}
            <tr>
                <td>{{ categorie_coureur.id }}</td>
                <td>{{ categorie_coureur.nomCategorie }}</td>
                <td>
                    <a href="{{ path('app_categorie_coureur_show', {'id': categorie_coureur.id}) }}">show</a>
                    <a href="{{ path('app_categorie_coureur_edit', {'id': categorie_coureur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_coureur_new') }}">Create new</a>
{% endblock %}
