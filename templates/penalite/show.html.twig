{% extends 'admin/base.html.twig' %}

{% block title %}Penalite{% endblock %}

{% block body %}
    <h1>Penalite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Equipe</th>
                <td>{{ penalite.equipe.nomEquipe }}</td>
            </tr>
            <tr>
                <th>Etape</th>
                <td>{{ penalite.etapeCourse.nomEtape }}</td>
            </tr>
            <tr>
                <th>Temps</th>
                <td>{{ penalite.temps}}</td>
            </tr>
        </tbody>
    </table>


    {# <a href="{{ path('app_penalite_edit', {'id': penalite.id}) }}">edit</a> #}
    {{ include('penalite/_delete_form.html.twig') }}<br>

    <a href="{{ path('app_penalite_index') }}"><button class="btn btn-outline-info btn-fw"> << Retour a la liste</button></a>
{% endblock %}
