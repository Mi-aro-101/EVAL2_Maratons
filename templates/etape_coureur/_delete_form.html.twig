<form method="post" action="{{ path('app_etape_coureur_delete', {'id': etape_coureur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etape_coureur.id) }}">
    <button class="btn">Delete</button>
</form>
