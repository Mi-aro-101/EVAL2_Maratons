<form method="post" action="{{ path('app_classement_categorie_delete', {'id': classement_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classement_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
